query total_counts {
    rateLimit {
        cost
        limit
    }
    repository(owner: "Azure", name: "azure-powershell") {
        pullRequests(first: 0) {
            totalCount
        }
        issues(first: 0) {
            totalCount
        }
    }
}
